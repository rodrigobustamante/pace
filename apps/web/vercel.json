{
  "installCommand": "cd ../.. && pnpm install",
  "buildCommand": "cd ../.. && pnpm --filter @pace/db exec prisma generate && pnpm --filter @pace/db db:migrate:deploy && pnpm --filter web build",
  "crons": [
    {
      "path": "/api/notifications/weekly",
      "schedule": "0 8 * * 1"
    }
  ]
}
